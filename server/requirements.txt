fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# LangChain + LangGraph (agentic orchestration)
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langgraph>=0.2.0

# Rate limiting support
slowapi>=0.1.9

# Web search
tavily-python>=0.3.0

# reportlab is a pure-Python library — no external binary needed
reportlab>=4.0.0

# Async support
anyio>=4.0.0

# Database support (PostgreSQL + SQLAlchemy)
SQLAlchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Clerk Authentication and HTTP requests
PyJWT[crypto]>=2.8.0
cryptography>=42.0.0
httpx>=0.27.0
